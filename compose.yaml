services:
  iris:
    image: ghcr.io/blockpass253/iris-health-fhir-query-explorer-iris:latest
    container_name: iris-fhir-query-explorer-iris
    ports:
      - "${IRIS_PORT:-1972}:1972"
      - "${IRIS_WEB_PORT:-52773}:52773"
